public class ChocolateBar implements Packable {
    /*
     * No need to create the empty constructor because java will create one itself
     */

    public double weight() {
        return 0.2;
    }
}
